<% provide(:title, "Modify project") %>
<% provide(:button_text, 'Save changes') %>

<h1>Modify Project</h1>

<%= render 'form', project: @project %>

<%= link_to 'Show', @project %> |
<%= link_to 'Back', projects_path %>
